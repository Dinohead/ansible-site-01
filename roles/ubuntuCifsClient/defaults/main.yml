---
#========================
# File defaults/main.yml
#
# Description: default variables for role
#
# Author: Derek 'dRock' Halsey
#========================

ubuntuCifsClient:
  user:
    name: "{{ ansible_user }}"
    password: "{{ vault_domainAdminPw }}"
  group:
    gid: 2473
    name: samba
  smbCredentialsDir: /root/smbcredentials
  mount: "{{ hostvar.mount }}"
#   mounts should contain a list of the form:
#    - name: video
#      server: spok
#      username: "{{ vault_domainAdminUser }}"
#      password: "{{ vault_domainAdminPw }}"
#      domain: "{{ domain }}"
#      permission: rw
